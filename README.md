DpkgFileListComparator
======================

This is a jar file that when run from command line will output differences between 2 files that were generated with dpkg --getselections. It will output in a nice way.

DpkgFileListComparator is a standalone java program that outputs the difference between 2 dpkg files in a nice format.
DPKG is a program/package manager for linux.
a dpkg file is the output of all installed programs with DPKG.
